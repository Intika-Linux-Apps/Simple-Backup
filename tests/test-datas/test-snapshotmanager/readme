Directory 'Template' contains data in different states used for the
creation of the snapshots.

Directory 'manager' contains data related to general tests of
snapshot manager.

Directory 'rebase' contains data related to re-base tests.


Note: The full snapshot of version 1 is corrupted - Needs to be replaced.

In version 2:
* File 'packages' is modified after creation of the snapshots in order to
  save some disk space.
* The stored creation times in snar file headers may differ between an
  incremental snapshot and the corresponding manual full snapshot used
  as reference result.

